version: '3.8'

services:
  mongo:
    image: mongo:6.0
    container_name: neomeet_mongo
    restart: unless-stopped
    # 1. Resource Limits (Security & Stability)
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    volumes:
      - mongo_data:/data/db
    networks:
      - neomeet_network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 20s # Thoda fast checking
      timeout: 5s
      retries: 5
      start_period: 30s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: neomeet_backend
    restart: unless-stopped
    # 2. Logging (Debugging ke liye)
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - ./backend/.env
    # PRODUCTION TIP: Avoid binding whole .env as volume if already in env_file
    expose:
      - "8000" # Ensure this matches your server.js port
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - neomeet_network
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_API_URL=${REACT_APP_API_URL}
        - REACT_APP_SOCKET_URL=${REACT_APP_SOCKET_URL}
    env_file:
      - ./frontend/.env
    container_name: neomeet_frontend
    restart: unless-stopped
    expose:
      - "80"
    depends_on:
      backend:
        condition: service_healthy # Wait for API to be ready
    networks:
      - neomeet_network

  nginx:
    image: nginx:alpine # 3. Alpine for smaller size
    container_name: neomeet_nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend
    networks:
      - neomeet_network

volumes:
  mongo_data:
    name: neomeet_mongo_data

networks:
  neomeet_network:
    name: neomeet_network
    driver: bridge
